---
layout: skel.hbs
bg-color: bg-white
---
<main class="vh-50-min font-sans">
    <div class="w-5/6 md:w-1/2 lg:w-1/3 ml-auto mr-auto ">
        <div class="markdown tracking-wide mb-5">
{{# if hasWarning }}
            <div class="text-center text font-medium p-3 bg-red-300">This post has been
                archived, restoration is in process.</div>
            {{/if}}
            
            {{# if title }}
            <h3 class="border-b border-blue-500"> {{ title }} </h3>
            {{/if}}
            {{# if hasDate }}
            <div class= "-mt-3 mb-3 text-sm text-gray-400 font-semibold">
                <time>{{ rfc3339 page.date }}</time>
            </div>
            {{/if}}
            {{{ content }}}</div>
    </div>
</main>
