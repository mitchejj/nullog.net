---
layout: skel.hbs
bg-color: bg-white
---
<main class="vh-50-min text-black font-sans">
    <div class="w-5/6 md:w-1/2 lg:w-1/3 ml-auto mr-auto ">
        <div class="markdown tracking-wide mb-5">
            {{# if title }}
            <h3 class="text-3xl border-b border-blue-500"> {{ title }} </h3>
            {{/if}}
            {{# if date }}
            <div class= "-mt-3 mb-3 text-sm text-gray-400 font-semibold">
                {{ short_date date }}
            </div> 
            {{/if}}
            {{{ content }}}</div>
    </div>
</main>
